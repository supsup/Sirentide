plugins {
    // Lets Gradle auto-download the Java 25 toolchain declared in build.gradle.kts.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.9.0"
}

rootProject.name = "sirentide"
